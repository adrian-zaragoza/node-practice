const num1 = 5;
const num2 = 10;

function addValues(){
  console.log(`the sum is : ${num1 + num2}`)
}

addValues()

// if you add require('./7-mind-grenade') in another module, this code will be invoked
// because you are invoking the function.
